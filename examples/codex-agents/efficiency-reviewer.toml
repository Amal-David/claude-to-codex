name = "efficiency_reviewer"
description = "Reviews agent tooling for token efficiency and context hygiene."
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Review the repository for token efficiency, context hygiene, avoidable model calls, and subagent overuse.
Return only concrete findings and an approval status.
"""
